# Resume_Score

Command to crate the environment:

python -m virtualenv .

Command to install all Libraries:

pip install streamlit python-docx docx2txt PyPDF2 pytesseract Pillow pdf2image openai regex fuzzywuzzy python-Levenshtein pandas numpy psycopg2-binary SQLAlchemy pgvector fastapi uvicorn python-multipart opencv-python python-dotenv

Command to run the file:

streamlit run streamlit_resume.py







